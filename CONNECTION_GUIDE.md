# 远程桌面控制 - 网络连接指南

## 当前状态
- 信令服务器: ✅ 运行中 (http://192.168.1.3:8000)
- 公网IP: 120.244.12.26 (中国移动)
- 局域网: ✅ 可访问
- 公网: ❌ 需要配置

## 手机连接方式

### 方式1: 手机连WiFi (同局域网)
最简单，无需额外配置。

**测试步骤:**
1. 手机连接与电脑相同的WiFi
2. 打开Android应用
3. 输入Token: 从服务器获取
4. 服务器地址: http://192.168.1.3:8000

**获取Token:**
```bash
curl -X POST http://192.168.1.3:8000/api/token
```

### 方式2: 手机用SIM卡 (需公网)
需要让手机能访问电脑的8000端口。

#### 方案A: ngrok内网穿透 (推荐，最简单)
```powershell
# 1. 下载ngrok
# https://ngrok.com/download

# 2. 运行（在另一个终端窗口）
ngrok http 8000

# 3. 复制显示的URL，例如: https://abc123.ngrok.io

# 4. 修改Windows端代码，使用ngrok URL作为信令服务器
```

#### 方案B: 配置Windows防火墙
```
1. Win + R, 输入: wf.msc
2. 入站规则 -> 新建规则
3. 端口 -> TCP -> 8000
4. 允许连接 -> 所有配置文件
5. 命名: RemoteDesktop-Signaling
```

#### 方案C: 路由器端口转发
```
1. 浏览器打开: http://192.168.1.1
2. 登录路由器
3. 端口转发/虚拟服务器
4. 添加规则: 外部8000 -> 内部192.168.1.3:8000
5. 保存
```

## 技术架构
```
手机(SIM卡) → 公网 → Windows公网IP:8000 → 路由器 → Windows(局域网):8000
                      ↑
                  信令服务器
                  (FastAPI)
```

## WebRTC P2P连接流程
1. Windows端生成offer → 上传到信令服务器
2. 手机端获取offer → 生成answer → 上传
3. 信令服务器转发
4. WebRTC建立P2P连接
5. 视频流传输

## 下一步行动
1. 先测试局域网连接（手机连WiFi）
2. 如果局域网正常，再配置公网访问
3. 推荐使用ngrok，5分钟搞定
