[project]
name = "signaling"
version = "1.0.0"
description = "Remote Desktop Signaling Server"
requires-python = ">=3.8"
dependencies = [
    "fastapi>=0.104.0",
    "uvicorn>=0.24.0",
    "python-multipart>=0.0.6",
    "pyngrok>=7.0.0",
]

[project.optional-dependencies]
dev = ["pyngrok>=7.0.0"]

[tool.uv]
# UV包管理器配置
